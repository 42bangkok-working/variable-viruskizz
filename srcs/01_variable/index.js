// define a constant [PI] with the value 3.14 (cannot be reassigned)

// define a mutable block-scoped variable [answer] with the value 42

// define a mutable function-scoped variable [count] with the value 0

// define a constant [SchoolOfCode] with the value '42Bangkok'

// log all variables to the console ordered by declaration

/* do not change the code below #####################################*/
module.exports = {
    PI, answer, count, SchoolOfCode
};
